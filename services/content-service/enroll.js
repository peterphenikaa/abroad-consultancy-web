const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function enroll() { const course = await prisma.course.findFirst(); if (!course) { console.log('Khong tim thay khoa hoc'); return; } console.log('Course ID:', course.courseId); const userId = '34e399cb-3dac-4868-9cd2-519ef32a4f81'; const enrollment = await prisma.enrollment.upsert({ where: { userId_courseId: { userId: userId, courseId: course.courseId } }, update: { status: 'ACTIVE' }, create: { userId: userId, courseId: course.courseId, status: 'ACTIVE' } }); console.log('Da Enroll: ', enrollment); } enroll().catch(console.error).finally(() => prisma.$disconnect());
