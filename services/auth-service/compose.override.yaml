#  compose.override.yaml
# this for
services:
  auth-service:
    ports:
      - '5555:5555'
    volumes:
      - ./:/usr/src/app
      - /usr/src/app/node_modules
    command: >
      sh -c "
        npx prisma generate &&
        npx prisma db push &&
        npm run dev
      "
