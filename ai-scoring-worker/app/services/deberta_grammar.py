"""
Chấm Grammatical Accuracy bằng DeBERTa (HuggingFace) — placeholder.

Khi triển khai thật: fine-tune / regression head trên rubric GRA, chạy local.
Trả về None để pipeline tạm dùng điểm GRA từ Gemini.
"""


def score_grammar_deberta(_essay: str) -> float | None:
    return None
